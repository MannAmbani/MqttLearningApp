<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TestingApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/top_background</item>
        <item name="android:textColorHint">@color/gray1</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:itemBackground">@color/background</item>
        <item name="refresh_color">@color/White_reverse</item>

        <!-- Customize your theme here. -->
    </style>
</resources>